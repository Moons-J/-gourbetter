<div class="container-small container mx-auto bg-white p-4">
  <div class="text-center mb-4"><%= image_tag 'logo', class: 'logo-form text-center' %></div>
  <h2 class="text-center">Log in</h2>
  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-inputs">
      <div class="mb-3">
        <%= f.input :email,
                    required: false,
                    autofocus: true,
                    input_html: { autocomplete: "email" } %>
      </div>
      <div class="mb-3">
        <%= f.input :password,
                    required: false,
                    input_html: { autocomplete: "current-password" } %>
      </div>
      <div class="mb-3">
        <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
      </div>
    </div>

    <div class="form-actions text-center">
      <%= f.button :submit, "Log in", class: "btn btn-yellow" %>
    </div>
  <% end %>
  <div class="mt-3 form-links-black text-center">
    <%= render "devise/shared/links" %>
  </div>
</div>
