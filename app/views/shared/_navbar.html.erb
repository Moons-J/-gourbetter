<nav class="bg-dark border-bottom border-body">
  <div class="container-fluid d-flex justify-content-between border-bottom">
    <%# logo div %>
    <div class="d-flex container align-items-center">
      <%= image_tag "brand", width: "50" %>
    </div>
    <%# search field div %>
    <div id="search-field" class="d-flex container align-items-center">
      <form class="d-flex flex-fill" role="search">
        <input class="form-control me-2 flex-fill" type="search" placeholder="Search" aria-label="Search" name="query" value="">
        <button class="btn btn-warning" type="submit">Search</button>
      </form>
    </div>
    <%# buttons field %>
    <div class="d-flex container align-items-center justify-content-end">
      <ul class="d-flex flex-row">
        <li class="nav-item">
          <%= link_to "Recipes", recipes_path %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Log Out", destroy_user_session_path %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Sign In", new_user_session_path %>
            <%= link_to "Sign Up", new_user_registration_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
